# Emergency kill switch. Keep false until hosted generic answers are intended.
PORTFOLIO_AI_ENABLE_GENERIC=false
# Server-side only. Never use a NEXT_PUBLIC_ prefix or commit a real value.
OPENAI_API_KEY=
# Set an approved OpenAI API model ID in the deployment environment.
OPENAI_MODEL=
